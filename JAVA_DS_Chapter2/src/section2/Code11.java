package section2;

/*클래스는 타입이다 설계도이다 실체가 아니다
따라서 클래스의 데이터 필드에 데이터를 저장할 수 없고 클래스의 멤버 메서드를 실행할 수 없다 실체가 아니므로
new 명령으로 해당 클래스 타입의 객체를 생성한 후, 그 객체에 데이터를 저장하고 메서드를 실행할 수 있다
예외가 존재하는데 바로 static 멤버이다
static 멤버는 클래스 안에 실재로 존재하며 객체에는 존재하지 않는다*/

/*static 멤버는 class 멤버이다*/

/*non static 멤버는 object 멤버이다*/

/*public : 클래스 외부에서 접근 가능
private : 클래스 내부에서 접근 가능
default : 동일 패키지에 있는 다른 클래스에서 접근 가능
protected : 동일 패키지의 다른 클래스와 다른 패키지의 하위클래스 에서도 접근 가능*/

/*data encapsulation or information hiding
모든 데이터 멤버를 private 으로 만들고 필요한 경우 public 한 get / set 메서드를 제공
이렇게 하면 객체가 제공하는 메서드를 통하지 않고서는 객체 내부의 데이터에 접근할 수 없다*/

public class Code11 {
}
